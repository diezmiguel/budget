APP_NAME="Orçamento"
APP_ENV=production
APP_KEY=
APP_DEBUG=false
APP_URL=https://budget.zeid10.com

APP_LOCALE=pt_BR
APP_FALLBACK_LOCALE=en
APP_FAKER_LOCALE=pt_BR

APP_MAINTENANCE_DRIVER=file

BCRYPT_ROUNDS=12

LOG_CHANNEL=stack
LOG_STACK=single
LOG_LEVEL=error

# --- Database (use the cPanel-prefixed names, e.g. usern_budget) ---
DB_CONNECTION=mysql
DB_HOST=chi209.greengeeks.net
DB_PORT=3306
DB_DATABASE=zeidcom_budget
DB_USERNAME=zeidcom_budget
DB_PASSWORD=?OoDR14t%Uz![9]^

# --- Sessions / SPA cookie auth (must match the real domain) ---
SESSION_DRIVER=database
SESSION_LIFETIME=120
SESSION_ENCRYPT=false
SESSION_PATH=/
SESSION_DOMAIN=budget.zeid10.com
SESSION_SECURE_COOKIE=true
SESSION_SAME_SITE=lax

SANCTUM_STATEFUL_DOMAINS=budget.zeid10.com

BROADCAST_CONNECTION=log
FILESYSTEM_DISK=local
QUEUE_CONNECTION=database
CACHE_STORE=database

MAIL_MAILER=log
MAIL_FROM_ADDRESS="no-reply@zeid10.com"
MAIL_FROM_NAME="${APP_NAME}"

VITE_APP_NAME="${APP_NAME}"

OPENAI_API_KEY=
OPENAI_RECEIPT_MODEL=gpt-4o-mini

