/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/hot
/public/storage
/storage/*.key
/storage/pail

# NOTE: /public/build and /vendor are intentionally NOT ignored.
# This project deploys to shared hosting (GreenGeeks) via git, where npm and
# composer cannot run, so the compiled frontend and PHP dependencies must be
# committed so `git pull` brings them to the server.
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed
